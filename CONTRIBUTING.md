# Contributing

## Setup
- Node.js + npm/pnpm
- `npm i`
- `npm run dev`

## Guidelines
- Keep core logic framework-agnostic
- Prefer pure functions in `src/core`
- Ensure message types are explicit (discriminated unions)
- Add tests for scoring/search changes
